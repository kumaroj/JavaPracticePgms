package searching;

public class BinarySearch {
    public boolean search(){
        int key = 6;
        int mid = 7;
        if (key == mid )
            return true;

        else if(key>mid)
            return search();

        else
            return search();

    }
}

